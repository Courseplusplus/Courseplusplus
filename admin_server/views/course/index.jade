//
   Created by wangzhaoyi on 16/7/8.

extends ../layout

block content
    table(class="table table-hover" style="margin-top:30px;")
        each item in list
            tr
                td
                    a(href="./course/#{item['course_id']}",class="link",style="color:#222;font-size:16px")
                        span(class="glyphicon glyphicon-folder-close")
                        span(style="margin-left:10px;") #{item["course_name"]}
                td
                    if item['is_closed'] == false
                        a(href="./course/#{item['course_id']}/close" value="close_course" )
                            button(class="btn btn-default")
                                span 关闭
                    else
                        span 已关闭