extends layout
block content

   //div(class="card")
   //   div(class="card-header")
   //      #{assignment.assignment_introduction}
   //   div(class="card-block")
   //      ul(class="list-group")
   //         each item in submits
   //            li(class="list-group-item" style="cursor: pointer" onclick="location.href='/course/#{course['course_id']}/assignment/#{item.assignment_id}';")
   //               span(class="label label-default label-pill pull-xs-right") 14
   //               #{item.file_name}

   form(enctype="multipart/form-data" action="/course/#{course.course_id}/assignment/#{assignment.assignment_id}/submit" method="post")
      "Select file to upload:"
      input(type="file" name="fileUpload" id="fileToUpload")
      input(type="submit" value="Upload Image" name="submit")

   h3 所有提交
      table(class="table table-hover" style="margin-top:30px;")
         each item in submits
            tr
               td
                  div(class="col-lg-12")
                     a(href="#",class="link col-lg-9",style="color:#222;font-size:14px")
                        span #{item["file_name"]}
                     a(class="link col-lg-3",href="/download/submit/#{item['submit_id']}" style="font-size:15px") 点击下载


   script(type='text/javascript').
      $('#side-class').attr({href: "/course/#{course['course_id']}"});
      $('#side-resource').attr({href: "/course/#{course['course_id']}/resource"});
      $('#side-assignment').attr({href: "/course/#{course['course_id']}/assignment"});
      $('#side-team').attr({href: "/course/#{course['course_id']}/team"});